PORT=5000
DATABASE_PATH=./data/triage.db
CLIENT_URL=http://localhost:5173

# ML inference service (ai-ml/inference/serve.py)
# Set to false to run on the rule engine only (no Python service needed).
USE_ML_SERVICE=false
ML_SERVICE_URL=http://localhost:8000
ML_TIMEOUT_MS=1500
